<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/css/style.css">
        <link rel="stylesheet" href="/css/alterar.css">
        <link rel="shortcut icon" href="/imgs/logo.png" type="image/x-icon">
        <link rel='stylesheet' href='https://cdn-uicons.flaticon.com/uicons-regular-rounded/css/uicons-regular-rounded.css'>
        <title>Recuperar senha</title>
    </head>
    <body>
        {{> preloader }}

        <form class="boxShadow" action="/auth/alterar" method="POST">
            <div class="senhaE boxShadow">
                <div class="topic">
                    <label for="senhaEemail">Email</label>
                    <div class="input">
                        <input type="email" name="senhaEEmail" id="senhaEEmail" placeholder="Insira o email" value={{emailenv}} disabled>
                        <input type="email" name="senhaEemail" value="{{emailenv}}" style="display:none;">
                        <i class="fi fi-rr-envelope"></i>
                    </div>
                </div>

                <div class="topic">
                    <label for="senhaEsenha">Senha nova</label>
                    <div class="input sinput">
                        <input type="password" name="senhaEsenha" id="senhaEsenha" placeholder="Insira a senha nova">
                        <i class="fi fi-rr-eye-crossed senha"></i>
                    </div>
                </div>
            </div>

            <style>
                div.boxShadow, form {
                    justify-content: center;
                }
                .topic, .senhaE {
                    margin: 20px;
                }
            </style>

            {{#if message}}
                <p class="alert">{{message}}</p>
            {{/if}}
            <input type="submit" class="btn criar" value="Alterar dados">
        </form>

        <script src="/js/login.js"></script>
    </body>
</html>
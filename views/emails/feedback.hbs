<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<style>
* {
    margin: 20px;
    padding: 0;
    border: 0;
    box-sizing: border-box;
    font-family: cursive;
    text-align: center;
}

h1, h2 {
    font-family: cursive;
    color: #65B0B5;
}

.linha {
    display: flex;
    justify-content: center;
    flex-direction: row;
    align-items: center;
    max-width: fit-content;
    margin: auto;
}

body {
    background-size: cover;
    background-attachment: fixed;
    display: flex;
    justify-content: center;
    flex-direction: column;

    width: 100%;
    border-radius: 20px;
}

p.span {
    font-size: 1.3em;
}

p.code {
    letter-spacing: 10px;
    padding: 10px 0 10px 10px;
    margin: 20px auto;
}

p.select {
    padding: 10px;
}

p.code, p.select {
    background-color: #B297FF;
    color: white;
    border-radius: 10px;
    display: flex;
    font-family: Arial, Helvetica, sans-serif;
    max-width: fit-content;
    font-weight: bolder;
    font-size: 1.5em;
}

p.footer {
    color: #0000005c;
    font-size: 0.8em;
}

p.txt {
    border: 3px solid #B297FF;
    max-width: fit-content;
    padding: 10px;
    margin: auto;
}
</style>
</head>
<body>
    <h1>Agradecemos pelo seu feedback!</h1>
    <p>Iremos verificar seu problema/feedback.</p>
    
    {{#if ItensProblema}}
        <p class="span">Suas escolhas foram:</p>
        <div class="linha">
            <p class="select">{{problema}}</p>
            <p class="select">→ {{ItensProblema}}</p>
        </div>
    {{else}}
        <p class="span">Sua escolha foi:</p>
        {{#if problema}}
            <div class="linha">
                <p class="select">{{problema}}</p>
            </div>
        {{else}}
            <div class="linha">
                <p class="select">Outros</p>
            </div>
        {{/if}}
    {{/if}}

    <p class="span">E sua descrição foi:</p>
    <p class="txt">"{{descricao}}"</p>

    <p class="footer">devoday</p>
</body>
</html>